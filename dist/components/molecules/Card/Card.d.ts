import { ReactNode } from 'react';

export interface CardProps {
    title?: ReactNode;
    description?: ReactNode;
    children?: ReactNode;
    footer?: ReactNode;
    className?: string;
}
export declare const Card: ({ title, description, children, footer, className }: CardProps) => import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=Card.d.ts.map